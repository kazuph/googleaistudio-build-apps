{
  "name": "顔切り出しアプリ",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "deploy": "pnpm run build && wrangler deploy --name face-cropper --assets ./dist"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "wrangler": "^4.17.0"
  }
}
